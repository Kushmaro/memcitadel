# Merge conflict resolution — upstream v3.3.2 → memcitadel

**Merge-base:** `2981433` (v3.1.0-era)
**Target:** `v3.3.2` (`cf0477b`)
**Conflicts:** 14 files

## Resolution decisions

| File | Strategy | Rationale |
|---|---|---|
| `.github/workflows/ci.yml` | fork wins | Keep lint-only CI; upstream's multi-matrix test job would re-introduce ChromaDB-bound tests. |
| `.gitignore` | fork wins | Fork had ES-specific ignores (`.env` with ES creds, etc.); upstream additions are incidental. |
| `README.md` (root) | fork wins | MemCitadel brand + ES architecture narrative; upstream README describes MemPalace only. Add "Upstream features compatibility" section in C7. |
| `mempalace/__init__.py` | fork wins | Fork's minimal version suppresses `elastic_transport` warnings; upstream's chromadb telemetry shim is irrelevant to ES. |
| `mempalace/cli.py` | upstream wins | Upstream added `sweeper`, `closet`, `diary`, `exporter`, `fact-check` subcommands. Fork's ES-specific `cmd_repair` rewrite is reverted here; C3 will rewrite `repair` to use the registry. Fork's migrations (`migrate_to_es.py`, `migrate_flat_to_wings.py`) are standalone scripts run as `python -m mempalace.migrate_*` — no cli.py wiring needed. |
| `mempalace/convo_miner.py` | upstream wins | Upstream added sweeper, PID guard, raised MAX_FILE_SIZE; fork had no semantic divergence. |
| `mempalace/layers.py` | upstream wins | |
| `mempalace/mcp_server.py` | upstream wins | Upstream added closet/diary tools. Fork's MCP server used the backend interface transparently. |
| `mempalace/miner.py` | upstream wins | Upstream added source-adapter scaffolding, None-metadata guards, size caps. |
| `mempalace/palace.py` | upstream wins (temporarily) | Takes upstream's rich `get_collection` with normalization schema versioning. ChromaBackend binding stays; **C3 rewrites to registry + ES default**. |
| `mempalace/palace_graph.py` | upstream wins | |
| `mempalace/searcher.py` | upstream wins | Upstream added closets support and `_first_or_empty` refactor. |
| `pyproject.toml` | 3-way | Fork `name="memcitadel"`, version bumped `0.2.0 → 0.3.0`, fork deps (`elasticsearch`, `pyyaml`, no chromadb), fork entry `memcitadel = "mempalace.cli:main"`. Upstream ruff config (`C901`, `max-complexity=25`, `extend-exclude=["benchmarks"]`) and `[project.entry-points."mempalace.backends"]` structure adopted but registers `elasticsearch = "mempalace.backends.elasticsearch:ElasticsearchBackend"` (the backend file is created in C4). |
| `tests/test_miner.py` | upstream wins | Will adapt if ES-specific breakage surfaces in C5. |

## Post-merge state

- `mempalace/backends/chroma.py` is present (from upstream). Will be deleted in C3.
- `mempalace/palace.py` imports `ChromaBackend` → importing `mempalace.palace` raises `ModuleNotFoundError: chromadb` in a venv without chromadb. C3 rewrites `palace.py`, `dedup.py`, `repair.py`, `migrate.py` to use the registry / ES default.
- `mempalace/es_client.py` is still present but unreferenced by upstream code. C4 moves its logic to `mempalace/backends/elasticsearch.py`.
- 41 ES integration tests still use positional args and dict access. C5 rewrites them to kwargs + typed results.
- Fork version 0.3.0, upstream wheel `mempalace`-packaged code paths.
